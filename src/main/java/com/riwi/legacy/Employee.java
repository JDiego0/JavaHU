package com.riwi.legacy;

/**
 * Clase Employee heredada de HU anteriores (sem. 1-4).
 * Forma parte de la jerarquía sellada (sealed) de Person.
 *
 * NOTA: No confundir con com.riwi.talent.model.Employee (record Java 17+)
 * que es la entidad principal del sistema MVC de esta HU.
 */
public final class Employee extends Person {
    // Clase placeholder requerida por la sealed class Person
}
